{
  "name": "astro-font-demo",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "cd packages/astro-font && pnpm build && cd ../../ && astro build",
    "preview": "astro preview",
    "fmt": "prettier --write '**/*' --ignore-unknown",
    "deploy-preview": "pnpm build && vercel deploy --prebuilt"
  },
  "devDependencies": {
    "prettier": "^3.1.1",
    "prettier-plugin-astro": "^0.12.2"
  },
  "dependencies": {
    "astro": "latest",
    "@astrojs/vercel": "latest",
    "astro-font": "workspace:./packages/astro-font"
  }
}
